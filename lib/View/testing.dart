import 'package:flutter/material.dart';

class tesing  extends StatelessWidget {
  const tesing ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("HEllo"),
    );
  }
}
